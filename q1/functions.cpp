#include <math.h>

double areaTriangle(double base, double heigh) {
	return (base * heigh) / 2;
}

double areaRetangle(double base, double heigh) {
	return base * heigh;
}

double areaCircle(double radius) {
	return M_PI * radius * radius;
}
